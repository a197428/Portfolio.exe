# Project knowledge

Add one directory per project with RU and EN Markdown documents. Record the problem, personal
contribution, technical decisions, measurable result, source URL, and live URL where publishable.

When repository inspection supports a claim, pin the repository, full commit SHA, and verification
date in structured source metadata. Keep implementation maturity precise: a verified production UI
does not by itself prove that the public snapshot calls a production API.
